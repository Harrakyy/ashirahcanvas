/**
 * OWNERSHIP: Bersama (kontrak)
 * Bentuk PrintArea (area print per zona). Dipakai lib/config & engine canvas.
 * Lihat ARCHITECTURE.md.
 */
export interface PrintArea {
  x: number
  y: number
  width: number
  height: number
}
